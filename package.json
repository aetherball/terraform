{
  "scripts": {
    "lint": "run-s lint:*",
    "lint:js": "prettier --check .",
    "format": "run-s format:*",
    "format:js": "prettier --ignore-unknown --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@aetherball/swarp": "^0.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.0.0",
    "npm-run-all2": "^8.0.0",
    "prettier": "^3.5.3"
  }
}
